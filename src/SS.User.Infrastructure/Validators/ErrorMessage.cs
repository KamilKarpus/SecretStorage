namespace SS.Users.Infrastructure.Validators
{
    public static class ErrorMessage
    {
        public static string PasswordError => "The password must contain one special character, digits and upper and lower case."; 
    }
}
